
	CREATE //dodawanie studenta
		POST /students
	
	READ //pobieranie wszystkich studentów
		GET /students


// CRUD dla studentów
	CREATE
		POST /students
	READ
		GET /students //wszyscy studenci
		GET /students/:id //student o danym Id
	UPDATE
		PATCH /students/:id
		PATCH /students // może być id studenta w ciele żądania
				//w ciele żądania
	DELETE
		DELETE /students/:id
// CRUD dla szkół
	CREATE
		POST /schools
	READ
		GET /schools//wszyscy studenci
		GET /schools/:id //student o danym Id
	UPDATE
		PATCH /schools/:id
		PATCH /schools // może być id studenta w ciele żądania
				
	DELETE
		DELETE /schools/:id

// Dodawanie kursów do szkoły
	POST /schools/:schoolId/courses

// Listowanie kursów w danej szkole, z wyszukiwaniem po nazwie i roku startu
	GET /schools/:schoolID/courses //wszystkie kursy w danej szkole
	GET /schools/:schoolID/courses?name=value1&startYear=value2 

// Dodawanie wykładów do kursu
	POST school/:schoolID/courses/:courseID/lecture

// Listowanie wykładów w ramach kursu w danej szkole
    Get /schools/:schoolID/course/:courseId/lectures

// Zapisywanie studentów na kurs w danej szkole
    POST /schools/:schoolId/courses/:courseId/students

    GET schools/:schoolId/courses/:courseId/students/:studentId
    GET /students/:id